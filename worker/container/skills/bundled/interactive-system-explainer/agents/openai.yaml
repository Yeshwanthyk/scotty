interface:
  display_name: "System Explainer"
  short_description: "Build source-grounded interactive explainers"
  default_prompt: "Use $interactive-system-explainer to build a source-grounded interactive HTML model of this system."
policy:
  allow_implicit_invocation: true
