interface:
  display_name: "Stateful Systems"
  short_description: "Model stateful systems and lifecycle behavior"
  default_prompt: "Use $stateful-systems to model the authoritative state, transitions, invariants, and proof for this workflow."
policy:
  allow_implicit_invocation: true
