diff --git a/node_modules/alchemy/lib/Apply.js b/node_modules/alchemy/lib/Apply.js
index a01c0fa..18f8ac5 100644
--- a/node_modules/alchemy/lib/Apply.js
+++ b/node_modules/alchemy/lib/Apply.js
@@ -136,6 +136,7 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
         value: {
             ...value,
             props: stripUnresolved(value.props),
+            bindings: stripUnresolved(value.bindings),
             namespace,
         },
     });
@@ -354,7 +355,7 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
                 resourceType: node.resource.Type,
                 props: news,
                 attr,
-                bindings: excludeDeletedBindings(node.bindings),
+                bindings: bindingOutputs,
                 providerVersion: node.provider.version ?? 0,
                 downstream: node.downstream,
                 removalPolicy: node.resource.RemovalPolicy,
@@ -461,7 +462,7 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
                     resourceType: node.resource.Type,
                     props: news,
                     attr,
-                    bindings: excludeDeletedBindings(node.bindings),
+                    bindings: bindingOutputs,
                     providerVersion: node.provider.version ?? 0,
                     downstream: node.downstream,
                     removalPolicy: node.resource.RemovalPolicy,
@@ -639,7 +640,7 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
                     props: news,
                     attr,
                     providerVersion: node.provider.version ?? 0,
-                    bindings: excludeDeletedBindings(node.bindings),
+                    bindings: bindingOutputs,
                     downstream: node.downstream,
                     removalPolicy: node.resource.RemovalPolicy,
                 });
@@ -656,7 +657,7 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
                     props: news,
                     attr,
                     providerVersion: node.provider.version ?? 0,
-                    bindings: excludeDeletedBindings(node.bindings),
+                    bindings: bindingOutputs,
                     downstream: node.downstream,
                     // Preserve the remaining backlog exactly as-is. GC is responsible for
                     // popping one generation at a time until the chain is exhausted.
@@ -901,10 +902,10 @@ const converge = Effect.fn(function* (plan, tracker, terminalStatuses, session,
                     logicalId,
                     instanceId,
                     resourceType: node.resource.Type,
-                    props: newProps,
+                    props: stripUnresolved(newProps),
                     attr,
                     providerVersion: node.provider.version ?? 0,
-                    bindings: excludeDeletedBindings(node.bindings),
+                    bindings: stripUnresolved(newBindings),
                     downstream: node.downstream,
                     namespace,
                     removalPolicy: node.resource.RemovalPolicy,
@@ -1052,6 +1053,7 @@ const collectGarbage = Effect.fn(function* (plan, session) {
                 value: {
                     ...value,
                     props: stripUnresolved(value.props),
+                    bindings: stripUnresolved(value.bindings),
                     namespace,
                 },
             });
diff --git a/node_modules/alchemy/lib/Cloudflare/Workers/LocalWorkerProvider.js b/node_modules/alchemy/lib/Cloudflare/Workers/LocalWorkerProvider.js
index f876789..47b465f 100644
--- a/node_modules/alchemy/lib/Cloudflare/Workers/LocalWorkerProvider.js
+++ b/node_modules/alchemy/lib/Cloudflare/Workers/LocalWorkerProvider.js
@@ -15,7 +15,7 @@ import * as Schema from "effect/Schema";
 import * as Scope from "effect/Scope";
 import * as Semaphore from "effect/Semaphore";
 import * as Stream from "effect/Stream";
-import { isResolved } from "../../Diff.js";
+import { isResolved, stripEffects } from "../../Diff.js";
 import * as RpcProvider from "../../Local/RpcProvider.js";
 import { Stack } from "../../Stack.js";
 import { sha256, unwrapRedacted } from "../../Util/index.js";
@@ -393,7 +393,8 @@ export const LocalWorkerProvider = () => RpcProvider.effect(Worker, LOCAL_ENTRY_
         // instance's fiber resolves to the Worker Attributes once it has
         // started, so enumerate that local state.
         list: () => Effect.forEach(Array.from(instances.values()), (instance) => Fiber.join(instance.fiber), { concurrency: "unbounded" }),
-        diff: Effect.fn(function* ({ id, news, newBindings, output }) {
+        diff: Effect.fn(function* ({ id, news: desired, newBindings, output }) {
+            const news = stripEffects(desired);
             if (!isResolved(news) || !isResolved(newBindings))
                 return undefined;
             const options = {
diff --git a/node_modules/alchemy/lib/Cloudflare/Workers/WorkerProvider.js b/node_modules/alchemy/lib/Cloudflare/Workers/WorkerProvider.js
index 320992f..601cba5 100644
--- a/node_modules/alchemy/lib/Cloudflare/Workers/WorkerProvider.js
+++ b/node_modules/alchemy/lib/Cloudflare/Workers/WorkerProvider.js
@@ -13,7 +13,7 @@ import * as crypto from "node:crypto";
 import { Unowned } from "../../AdoptPolicy.js";
 import * as Artifacts from "../../Artifacts.js";
 import { hashDirectory } from "../../Command/Memo.js";
-import { isResolved } from "../../Diff.js";
+import { isResolved, stripEffects } from "../../Diff.js";
 import * as ProviderLayer from "../../Local/ProviderLayer.js";
 import * as Provider from "../../Provider.js";
 import {} from "../../Resource.js";
@@ -1564,8 +1564,9 @@ export const LiveWorkerProvider = () => Provider.effect(Worker, Effect.gen(funct
                 ]
                 : []))));
         }),
-        diff: Effect.fn(function* ({ id, news, olds, output, newBindings }) {
+        diff: Effect.fn(function* ({ id, news: desired, olds, output, newBindings }) {
             const { accountId } = yield* yield* CloudflareEnvironment;
+            const news = stripEffects(desired);
             if (!isResolved(news))
                 return undefined;
             if ((output?.accountId ?? accountId) !== accountId) {
diff --git a/node_modules/alchemy/lib/Diff.js b/node_modules/alchemy/lib/Diff.js
index 4dbe465..6152c55 100644
--- a/node_modules/alchemy/lib/Diff.js
+++ b/node_modules/alchemy/lib/Diff.js
@@ -61,6 +61,23 @@ const _stripUnresolved = (value) => {
     }
     return value;
 };
+export const stripEffects = (value) => _stripEffects(value);
+const _stripEffects = (value) => {
+    if (value == null || isPrimitive(value))
+        return value;
+    if (Output.isExpr(value))
+        return value;
+    if (Effect.isEffect(value))
+        return undefined;
+    if (Redacted.isRedacted(value) || Duration.isDuration(value))
+        return value;
+    if (Array.isArray(value))
+        return value.map(_stripEffects);
+    if (typeof value === "object") {
+        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, _stripEffects(item)]));
+    }
+    return value;
+};
 export const somePropsAreDifferent = (olds, news, props) => {
     for (const prop of props) {
         if (olds[prop] !== news[prop]) {
@@ -83,8 +100,8 @@ export const anyPropsAreDifferent = (olds, news) => {
     return false;
 };
 export const havePropsChanged = (oldProps, newProps) => Output.hasOutputs(newProps) ||
-    JSON.stringify(canonicalize(oldProps ?? {}, false)) !==
-        JSON.stringify(canonicalize(newProps ?? {}, false));
+    JSON.stringify(canonicalize(stripUnresolved(oldProps ?? {}), false)) !==
+        JSON.stringify(canonicalize(stripUnresolved(newProps ?? {}), false));
 /**
  * Sort-keys deep equality for plain data (objects, arrays, primitives).
  * Use in provider `diff` handlers instead of ad-hoc `JSON.stringify` comparisons.
@@ -114,21 +131,17 @@ const canonicalize = (value, stripNullish) => {
     }
     return value;
 };
+const bySid = (a, b) => a.sid < b.sid ? -1 : a.sid > b.sid ? 1 : 0;
+export const sortBindings = (bindings) => [...bindings].sort(bySid);
 /**
- * Collapse bindings that share the same `sid`, keeping the last occurrence.
- *
- * The same binding can be recorded more than once on a target resource — e.g.
- * a KV namespace bound to both a Worker and a Workflow ends up pushed twice to
- * `stack.bindings[fqn]`. `diffBindings` already collapses these implicitly via
- * its `Map` keyed by `sid`, so the `reconcile` path never observes duplicates.
- * Use this helper to give a provider's `diff` handler the same de-duplicated
- * binding set, keeping plan-time hashing consistent with deploy-time.
+ * Collapse bindings that share the same `sid`, keeping the last occurrence,
+ * and return them in deterministic (sid-sorted) order.
  */
-export const dedupeBindings = (bindings) => Array.from(new Map(bindings.map((b) => [b.sid, b])).values());
+export const dedupeBindings = (bindings) => sortBindings(Array.from(new Map(bindings.map((b) => [b.sid, b])).values()));
 export const diffBindings = (oldBindings, newBindings) => {
     const oldMap = new Map(oldBindings.map((b) => [b.sid, b]));
     const newMap = new Map(newBindings.map((b) => [b.sid, b]));
-    return [
+    return sortBindings([
         ...Array.from(oldMap)
             .filter(([sid]) => !newMap.has(sid))
             .map(([sid, old]) => ({
@@ -148,5 +161,5 @@ export const diffBindings = (oldBindings, newBindings) => {
                 data: binding.data,
             };
         }),
-    ];
+    ]);
 };
diff --git a/node_modules/alchemy/lib/Plan.js b/node_modules/alchemy/lib/Plan.js
index 81d309a..687a5e2 100644
--- a/node_modules/alchemy/lib/Plan.js
+++ b/node_modules/alchemy/lib/Plan.js
@@ -85,9 +85,7 @@ Effect.gen(function* () {
                 const oldProps = oldState.status === "updating"
                     ? oldState.old.props
                     : oldState.props;
-                const oldBindings = oldState.bindings ?? [];
-                // Collapse duplicate bindings by sid so the binding set handed to
-                // `diff` matches what `reconcile` receives (see `dedupeBindings`).
+                const oldBindings = dedupeBindings(oldState.bindings ?? []);
                 const newBindings = dedupeBindings(stack.bindings[resource.FQN] ?? []);
                 const diff = yield* provider.diff
                     ? provider
@@ -501,7 +499,7 @@ Effect.gen(function* () {
                 forceUpdateAfterAdoption = true;
             }
         }
-        const oldBindings = oldState?.bindings ?? [];
+        const oldBindings = dedupeBindings(oldState?.bindings ?? []);
         const bindingDiffs = diffBindings(oldBindings, newBindings);
         const Node = (node) => ({
             ...node,
diff --git a/node_modules/alchemy/src/Apply.ts b/node_modules/alchemy/src/Apply.ts
index a025189..ca1c7ba 100644
--- a/node_modules/alchemy/src/Apply.ts
+++ b/node_modules/alchemy/src/Apply.ts
@@ -414,6 +414,7 @@ const executeNode = (
         value: {
           ...value,
           props: stripUnresolved(value.props),
+          bindings: stripUnresolved(value.bindings),
           namespace,
         } as S,
       });
@@ -680,7 +681,7 @@ const executeNode = (
           resourceType: node.resource.Type,
           props: news,
           attr,
-          bindings: excludeDeletedBindings(node.bindings),
+          bindings: bindingOutputs,
           providerVersion: node.provider.version ?? 0,
           downstream: node.downstream,
           removalPolicy: node.resource.RemovalPolicy,
@@ -812,7 +813,7 @@ const executeNode = (
             resourceType: node.resource.Type,
             props: news,
             attr,
-            bindings: excludeDeletedBindings(node.bindings),
+            bindings: bindingOutputs,
             providerVersion: node.provider.version ?? 0,
             downstream: node.downstream,
             removalPolicy: node.resource.RemovalPolicy,
@@ -1039,7 +1040,7 @@ const executeNode = (
             props: news,
             attr,
             providerVersion: node.provider.version ?? 0,
-            bindings: excludeDeletedBindings(node.bindings),
+            bindings: bindingOutputs,
             downstream: node.downstream,
             removalPolicy: node.resource.RemovalPolicy,
           });
@@ -1055,7 +1056,7 @@ const executeNode = (
             props: news,
             attr,
             providerVersion: node.provider.version ?? 0,
-            bindings: excludeDeletedBindings(node.bindings),
+            bindings: bindingOutputs,
             downstream: node.downstream,
             // Preserve the remaining backlog exactly as-is. GC is responsible for
             // popping one generation at a time until the chain is exhausted.
@@ -1419,10 +1420,10 @@ const converge = Effect.fn(function* (
           logicalId,
           instanceId,
           resourceType: node.resource.Type,
-          props: newProps,
+          props: stripUnresolved(newProps),
           attr,
           providerVersion: node.provider.version ?? 0,
-          bindings: excludeDeletedBindings(node.bindings),
+          bindings: stripUnresolved(newBindings),
           downstream: node.downstream,
           namespace,
           removalPolicy: node.resource.RemovalPolicy,
@@ -1614,6 +1615,7 @@ const collectGarbage = Effect.fn(function* (
             value: {
               ...value,
               props: stripUnresolved(value.props),
+              bindings: stripUnresolved(value.bindings),
               namespace,
             } as S,
           });
diff --git a/node_modules/alchemy/src/Cloudflare/Workers/LocalWorkerProvider.ts b/node_modules/alchemy/src/Cloudflare/Workers/LocalWorkerProvider.ts
index cf6f1dc..68f130f 100644
--- a/node_modules/alchemy/src/Cloudflare/Workers/LocalWorkerProvider.ts
+++ b/node_modules/alchemy/src/Cloudflare/Workers/LocalWorkerProvider.ts
@@ -57,7 +57,7 @@ import * as Scope from "effect/Scope";
 import * as Semaphore from "effect/Semaphore";
 import * as Stream from "effect/Stream";
 import type * as Bundle from "../../Bundle/Bundle.ts";
-import { isResolved } from "../../Diff.ts";
+import { isResolved, stripEffects } from "../../Diff.ts";
 import * as RpcProvider from "../../Local/RpcProvider.ts";
 import type { ResourceBinding } from "../../Resource.ts";
 import { Stack } from "../../Stack.ts";
@@ -600,7 +600,8 @@ export const LocalWorkerProvider = () =>
             (instance) => Fiber.join(instance.fiber),
             { concurrency: "unbounded" },
           ),
-        diff: Effect.fn(function* ({ id, news, newBindings, output }) {
+        diff: Effect.fn(function* ({ id, news: desired, newBindings, output }) {
+          const news = stripEffects(desired);
           if (!isResolved(news) || !isResolved(newBindings)) return undefined;
           const options = {
             id,
diff --git a/node_modules/alchemy/src/Cloudflare/Workers/WorkerProvider.ts b/node_modules/alchemy/src/Cloudflare/Workers/WorkerProvider.ts
index 6ae1973..bc6ef7b 100644
--- a/node_modules/alchemy/src/Cloudflare/Workers/WorkerProvider.ts
+++ b/node_modules/alchemy/src/Cloudflare/Workers/WorkerProvider.ts
@@ -14,7 +14,7 @@ import { Unowned } from "../../AdoptPolicy.ts";
 import * as Artifacts from "../../Artifacts.ts";
 import type { ScopedPlanStatusSession } from "../../Cli/Cli.ts";
 import { hashDirectory, type MemoOptions } from "../../Command/Memo.ts";
-import { isResolved } from "../../Diff.ts";
+import { isResolved, stripEffects } from "../../Diff.ts";
 import * as ProviderLayer from "../../Local/ProviderLayer.ts";
 import * as Provider from "../../Provider.ts";
 import { type ResourceBinding } from "../../Resource.ts";
@@ -2172,8 +2172,15 @@ export const LiveWorkerProvider = () =>
               ),
             );
           }),
-        diff: Effect.fn(function* ({ id, news, olds, output, newBindings }) {
+        diff: Effect.fn(function* ({
+          id,
+          news: desired,
+          olds,
+          output,
+          newBindings,
+        }) {
           const { accountId } = yield* yield* CloudflareEnvironment;
+          const news = stripEffects(desired);
           if (!isResolved(news)) return undefined;
           if ((output?.accountId ?? accountId) !== accountId) {
             return { action: "replace" };
diff --git a/node_modules/alchemy/src/Diff.ts b/node_modules/alchemy/src/Diff.ts
index ecf2bbd..3e801b3 100644
--- a/node_modules/alchemy/src/Diff.ts
+++ b/node_modules/alchemy/src/Diff.ts
@@ -86,6 +86,22 @@ const _stripUnresolved = (value: unknown): unknown => {
   return value;
 };
 
+export const stripEffects = <T>(value: T): T => _stripEffects(value) as T;
+
+const _stripEffects = (value: unknown): unknown => {
+  if (value == null || isPrimitive(value)) return value;
+  if (Output.isExpr(value)) return value;
+  if (Effect.isEffect(value)) return undefined;
+  if (Redacted.isRedacted(value) || Duration.isDuration(value)) return value;
+  if (Array.isArray(value)) return value.map(_stripEffects);
+  if (typeof value === "object") {
+    return Object.fromEntries(
+      Object.entries(value).map(([key, item]) => [key, _stripEffects(item)]),
+    );
+  }
+  return value;
+};
+
 export const somePropsAreDifferent = <Props extends Record<string, any>>(
   olds: Props,
   news: Props,
@@ -121,8 +137,8 @@ export const havePropsChanged = <Props extends object>(
   newProps: Props,
 ) =>
   Output.hasOutputs(newProps) ||
-  JSON.stringify(canonicalize(oldProps ?? {}, false)) !==
-    JSON.stringify(canonicalize(newProps ?? {}, false));
+  JSON.stringify(canonicalize(stripUnresolved(oldProps ?? {}), false)) !==
+    JSON.stringify(canonicalize(stripUnresolved(newProps ?? {}), false));
 
 export type DeepEqualOptions = {
   /**
@@ -173,18 +189,18 @@ const canonicalize = (value: unknown, stripNullish: boolean): unknown => {
   return value;
 };
 
+const bySid = (a: { sid: string }, b: { sid: string }): number =>
+  a.sid < b.sid ? -1 : a.sid > b.sid ? 1 : 0;
+
+export const sortBindings = <B extends { sid: string }>(bindings: B[]): B[] =>
+  [...bindings].sort(bySid);
+
 /**
- * Collapse bindings that share the same `sid`, keeping the last occurrence.
- *
- * The same binding can be recorded more than once on a target resource — e.g.
- * a KV namespace bound to both a Worker and a Workflow ends up pushed twice to
- * `stack.bindings[fqn]`. `diffBindings` already collapses these implicitly via
- * its `Map` keyed by `sid`, so the `reconcile` path never observes duplicates.
- * Use this helper to give a provider's `diff` handler the same de-duplicated
- * binding set, keeping plan-time hashing consistent with deploy-time.
+ * Collapse bindings that share the same `sid`, keeping the last occurrence,
+ * and return them in deterministic (sid-sorted) order.
  */
 export const dedupeBindings = <B extends ResourceBinding>(bindings: B[]): B[] =>
-  Array.from(new Map(bindings.map((b) => [b.sid, b])).values());
+  sortBindings(Array.from(new Map(bindings.map((b) => [b.sid, b])).values()));
 
 export const diffBindings = (
   oldBindings: ResourceBinding[],
@@ -192,7 +208,7 @@ export const diffBindings = (
 ): BindingNode[] => {
   const oldMap = new Map(oldBindings.map((b) => [b.sid, b]));
   const newMap = new Map(newBindings.map((b) => [b.sid, b]));
-  return [
+  return sortBindings([
     ...Array.from(oldMap)
       .filter(([sid]) => !newMap.has(sid))
       .map(([sid, old]) => ({
@@ -212,5 +228,5 @@ export const diffBindings = (
         data: binding.data,
       };
     }),
-  ];
+  ]);
 };
diff --git a/node_modules/alchemy/src/Plan.ts b/node_modules/alchemy/src/Plan.ts
index 58e8e61..14e4706 100644
--- a/node_modules/alchemy/src/Plan.ts
+++ b/node_modules/alchemy/src/Plan.ts
@@ -313,9 +313,7 @@ export const make = <A>(
                   ? oldState.old.props
                   : oldState.props;
 
-              const oldBindings = oldState.bindings ?? [];
-              // Collapse duplicate bindings by sid so the binding set handed to
-              // `diff` matches what `reconcile` receives (see `dedupeBindings`).
+              const oldBindings = dedupeBindings(oldState.bindings ?? []);
               const newBindings = dedupeBindings(
                 stack.bindings[resource.FQN] ?? [],
               );
@@ -802,7 +800,7 @@ export const make = <A>(
               }
             }
 
-            const oldBindings = oldState?.bindings ?? [];
+            const oldBindings = dedupeBindings(oldState?.bindings ?? []);
             const bindingDiffs = diffBindings(oldBindings, newBindings);
 
             const Node = <T extends Apply>(
